[package]
name = "holochain_chc"
version = "0.1.0-dev.1"
description = "TODO"
license = "Apache-2.0"
homepage = "https://github.com/holochain/holochain"
documentation = "https://docs.rs/holochain_chc"
authors = ["Holochain Core Dev Team <devcore@holochain.org>"]
edition = "2021"

# reminder - do not use workspace deps
[dependencies]
async-trait = "0.1"
holochain_keystore = { version = "^0.4.0-dev.18", path = "../holochain_keystore", default-features = false }
holochain_serialized_bytes = "=0.0.55"
holochain_types = { path = "../holochain_types", version = "^0.4.0-dev.20" }
reqwest = { version = "0.12", features = ["json", "native-tls-vendored"] }
serde = { version = "1.0", features = ["derive"] }
url = "2.4"
url2 = "0.0.6"


[dev-dependencies]
fixt = { version = "^0.4.0-dev.3", path = "../fixt" }
pretty_assertions = "1.4"
tokio = { version = "1.36.0", features = ["full"] }

[lints]
workspace = true

[features]
