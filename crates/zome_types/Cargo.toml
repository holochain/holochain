[package]
name = "holochain_zome_types"
version = "0.0.1"
description = "Holochain zome types"
license = "GPL-3.0-only"
homepage = "https://github.com/Holo-host/holochain-2020"
documentation = "https://github.com/Holo-host/holochain-2020"
readme = "README.md"
authors = [ "Holochain Core Dev Team <devcore@holochain.org>" ]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
holo_hash_core = { path = "../holo_hash_core" }
holochain_serialized_bytes = "=0.0.39"
# TODO: remove and replace with HoloHash
multihash = "0.8.0"
# TODO: remove this once there is some less obtrusive source of randomness
nanoid = "0.3"
# TODO: remove and replace with HoloHash
rust-base58 = "0.0.4"
serde = { version = "1.0.104", features = [ "derive" ] }
