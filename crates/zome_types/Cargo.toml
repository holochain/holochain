[package]
name = "sx_zome_types"
version = "0.0.1"
description = "Holochain zome types"
license = "GPL-3.0-only"
homepage = "https://github.com/Holo-host/holochain-2020"
documentation = "https://github.com/Holo-host/holochain-2020"
authors = ["Holochain Core Dev Team <devcore@holochain.org>"]
edition = "2018"

# !!! IMPORTANT !!!
# everything you put here will be __compiled into every wasm everywhere__
# think very carefully before adding anything at all
[dependencies]
holochain_serialized_bytes = "=0.0.36"
serde = { version = "1.0.104", features = ["derive", "rc"] }
sx_address_types = { version = "0.0.1", path = "../address_types" }
