use crate::DhtArc;

#[test]
fn a_test() {
    // todo
}

struct EpochStats {
    net_change: f64,
    gross_change: f64,
    min_redundancy: u32,
}

fn one_epoch(mut arcs: Vec<DhtArc>) -> EpochStats {
    todo!()
}
