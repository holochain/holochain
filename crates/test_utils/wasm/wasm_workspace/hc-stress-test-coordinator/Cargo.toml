[package]
name = "files"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name = "files"

[dependencies]
hdk = { workspace = true }

serde = { workspace = true }

files_integrity = { workspace = true }
