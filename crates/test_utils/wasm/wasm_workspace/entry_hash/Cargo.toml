[package]
name = "test_wasm_entry_hash"
version = "0.0.1"
authors = [ "thedavidmeister", "thedavidmeister@gmail.com" ]
edition = "2018"

[lib]
name = "test_wasm_entry_hash"
crate-type = [ "cdylib", "rlib" ]

[dependencies]
hdk3 = { path = "../../../../hdk" }
serde = "=1.0.104"
