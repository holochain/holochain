[package]
name = "test_wasm_dna_properties"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
name = "test_wasm_dna_properties"
crate-type = ["cdylib", "rlib"]

[[example]]
name = "integrity_test_wasm_dna_properties"
path = "src/integrity.rs"
crate-type = ["cdylib", "rlib"]

# reminder - do not use workspace deps
[dependencies]
hdk = { version = "=0.9.0-dev.0", optional = true }
serde = "1.0"
holochain_serialized_bytes = "0.0.57"
hdi = { version = "=0.9.0-dev.4" }
holochain_test_wasm_common = { path = "../../../wasm_common" }

[dev-dependencies]
hdk = { version = "=0.9.0-dev.0", features = ["fixturators"] }
fixt = { version = "=0.9.0-dev.4" }

[features]
default = ["hdk"]
integrity = []
mock = ["hdk/mock"]
