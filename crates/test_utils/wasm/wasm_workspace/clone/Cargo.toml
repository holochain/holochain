[package]
name = "test_wasm_clone"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
name = "test_wasm_clone"
crate-type = ["cdylib", "rlib"]

[[example]]
name = "integrity_test_wasm_clone"
path = "src/integrity.rs"
crate-type = ["cdylib", "rlib"]

# reminder - do not use workspace deps
[dependencies]
serde = "1.0"
holochain_serialized_bytes = "0.0.57"
hdk = { version = "0.9.0-dev.1", optional = true, features = ["properties"] }
hdi = { version = "0.9.0-dev.4" }

[features]
default = ["hdk"]
integrity = []
