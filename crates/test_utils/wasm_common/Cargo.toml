[package]
name = "holochain_test_wasm_common"
version = "0.0.2"
authors = [ "thedavidmeister", "thedavidmeister@gmail.com" ]
edition = "2018"
description = "Common code for Wasm testing for Holochain"
license = "Apache-2.0"

[lib]
name = "holochain_test_wasm_common"
crate-type = [ "cdylib", "rlib" ]
path = "src/lib.rs"

[dependencies]
hdk = { path = "../../hdk", version = "0.0.102"}
serde = "1.0"
