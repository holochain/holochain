[package]
name = "holochain_state_types"
version = "0.6.0-dev.18"
description = "Types for the holochain_state crate"
license = "Apache-2.0"
repository = "https://github.com/holochain/holochain"
documentation = "https://docs.rs/holochain_state_types"
authors = ["Holochain Core Dev Team <devcore@holochain.org>"]
edition = "2021"

# reminder - do not use workspace deps
[dependencies]
serde = { version = "1.0", features = ["derive"] }
holochain_integrity_types = { version = "^0.6.0-dev.18", path = "../holochain_integrity_types", default-features = false }
holo_hash = { version = "^0.6.0-dev.16", path = "../holo_hash" }

[lints]
workspace = true
