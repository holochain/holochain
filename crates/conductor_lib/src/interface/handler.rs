#![allow(dead_code)]

// TODO: use jsonrpc-lite or something to represent JSON-RPC requests
struct JsonRpc;

struct InterfaceJsonRpcHandler;

impl InterfaceJsonRpcHandler {
    pub fn handle(_msg: JsonRpc) {}
}
