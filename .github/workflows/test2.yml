name: test2

on:
  pull_request: {}

jobs:
  datachannel:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: build
        run: |
          clang --version
          perl --version
          cmake --version
          
          cd crates/holochain_p2p
          cargo build
